# Image Loading System - Visual Guide

## 🔄 Flow Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                    User Starts Quiz                          │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│            Question Generated with Image Data                │
│  • Primary URL (SuperHero API)                              │
│  • Fallback URLs array (CDN + formats)                      │
│  • Character name (for placeholder)                          │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│              Display Loading State                           │
│  🔄 Spinner with "Loading character..." text                │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│          Try Primary URL (SuperHero API)                     │
│  ⏱️ 3-second timeout                                         │
└───────┬───────────────────────────────────────┬─────────────┘
        │ SUCCESS                               │ FAIL
        ▼                                       ▼
┌──────────────────┐              ┌──────────────────────────┐
│  Display Image   │              │   Try CDN URL (.jpg)     │
│  • Fade in       │              │   ⏱️ 3-second timeout     │
│  • Remove loader │              └──────┬───────────────┬───┘
└──────────────────┘                     │ SUCCESS       │ FAIL
                                         ▼               ▼
                              ┌──────────────┐  ┌────────────────┐
                              │Display Image │  │ Try CDN (.png) │
                              └──────────────┘  └────┬───────┬───┘
                                                     │ OK    │ FAIL
                                                     ▼       ▼
                                          ┌──────────────┐ ┌──────────┐
                                          │Display Image │ │Try .webp │
                                          └──────────────┘ └────┬─────┘
                                                               │ FAIL
                                                               ▼
                                                    ┌────────────────────┐
                                                    │ Show SVG           │
                                                    │ Placeholder        │
                                                    │ ✅ Always succeeds │
                                                    └────────────────────┘
```

## 🎨 Component Architecture

```
┌──────────────────────────────────────────────────────────────┐
│                     UIController                              │
│  ┌────────────────────────────────────────────────────────┐  │
│  │            displayQuestion(question)                    │  │
│  │  • Shows question text                                 │  │
│  │  • Calls loadCharacterImage()                          │  │
│  │  • Creates answer buttons                              │  │
│  └───────────────────────┬────────────────────────────────┘  │
│                          │                                    │
│  ┌───────────────────────▼────────────────────────────────┐  │
│  │     loadCharacterImage(primaryUrl, fallbackUrls)       │  │
│  │  • Creates loading container                           │  │
│  │  • Implements fallback chain                           │  │
│  │  • Handles timeouts & errors                           │  │
│  │  • Shows success animations                            │  │
│  └───────────────────────┬────────────────────────────────┘  │
│                          │                                    │
│                ┌─────────┴─────────┐                          │
│                │ Success           │ All Failed               │
│                ▼                   ▼                          │
│  ┌──────────────────┐   ┌──────────────────────┐            │
│  │  Display Image   │   │ showImagePlaceholder()│            │
│  │  • Fade in       │   │  • Creates SVG       │            │
│  │  • Remove loader │   │  • Marvel branding   │            │
│  └──────────────────┘   │  • Character name    │            │
│                          └──────────────────────┘            │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│                  QuestionGenerator                            │
│  ┌────────────────────────────────────────────────────────┐  │
│  │          createNameQuestion(character)                  │  │
│  │  • Calls getCharacterImageUrl()                        │  │
│  │  • Calls getCharacterImageFallbacks()                  │  │
│  │  • Returns question with image data                    │  │
│  └───────────────────────┬────────────────────────────────┘  │
│                          │                                    │
│  ┌───────────────────────▼────────────────────────────────┐  │
│  │    getCharacterImageUrl(character)                      │  │
│  │  • Extracts primary URL                                │  │
│  │  • Handles path + extension formats                    │  │
│  └─────────────────────────────────────────────────────────┘  │
│                          │                                    │
│  ┌───────────────────────▼────────────────────────────────┐  │
│  │    getCharacterImageFallbacks(character)                │  │
│  │  • Builds fallback URL array                           │  │
│  │  • Includes API fallbacks                              │  │
│  │  • Adds format alternatives                            │  │
│  │  • Creates SVG placeholder                             │  │
│  └─────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────┘
```

## 📊 Data Flow

```
API Response
    │
    ├─ character.thumbnail.path = "https://api.com/image.jpg"
    ├─ character.imageFallbacks = [...]
    │
    ▼
QuestionGenerator.createNameQuestion()
    │
    ├─ getCharacterImageUrl()
    │   └─ Returns: "https://api.com/image.jpg"
    │
    ├─ getCharacterImageFallbacks()
    │   └─ Returns: [
    │       "https://cdn.jsdelivr.net/.../image.jpg",
    │       "https://cdn.jsdelivr.net/.../image.png",
    │       "data:image/svg+xml,..."
    │     ]
    │
    ▼
Question Object
    │
    ├─ question: "Who is this character?"
    ├─ image: "https://api.com/image.jpg"
    ├─ imageFallbacks: [...]
    ├─ answers: [...]
    │
    ▼
UIController.displayQuestion()
    │
    ├─ Shows question text
    ├─ Calls loadCharacterImage(image, imageFallbacks)
    │   │
    │   ├─ Try image (3s timeout)
    │   ├─ Try fallbacks[0] (3s timeout)
    │   ├─ Try fallbacks[1] (3s timeout)
    │   ├─ Try fallbacks[2] (3s timeout)
    │   └─ Show placeholder (always succeeds)
    │
    └─ Creates answer buttons
```

## 🎯 State Transitions

```
STATE: LOADING
┌─────────────────────┐
│  🔄 Spinner visible │
│  Image: hidden      │
│  Trying: URL #1     │
└──────┬──────────────┘
       │
       ├─ SUCCESS ──────────────┐
       │                        │
       └─ FAIL/TIMEOUT          │
          ┌────────────────┐    │
          │ Try next URL   │    │
          └────────────────┘    │
                                │
                                ▼
                    STATE: DISPLAYING
                    ┌─────────────────────┐
                    │  ✅ Image visible   │
                    │  Spinner: hidden    │
                    │  Animation: fade-in │
                    └─────────────────────┘
                                │
                                │ All URLs failed
                                ▼
                    STATE: PLACEHOLDER
                    ┌─────────────────────┐
                    │  🎨 SVG visible     │
                    │  Spinner: hidden    │
                    │  Branded design     │
                    └─────────────────────┘
```

## 🔍 Error Handling Matrix

```
┌──────────────────┬────────────────────┬─────────────────────┐
│   Error Type     │   Detection       │    Response         │
├──────────────────┼────────────────────┼─────────────────────┤
│ Network Fail     │ img.onerror       │ Try next fallback   │
│ Timeout (>3s)    │ setTimeout()      │ Try next fallback   │
│ CORS Error       │ img.onerror       │ Try CDN fallback    │
│ 404 Not Found    │ img.onerror       │ Try next fallback   │
│ Invalid URL      │ Validation        │ Skip to next        │
│ All URLs Fail    │ Array exhausted   │ Show placeholder    │
└──────────────────┴────────────────────┴─────────────────────┘
```

## 🎨 CSS Classes

```
#character-image
    ├─ .image-container
    │   ├─ .image-loading (position: absolute)
    │   │   ├─ .spinner-border
    │   │   └─ <p> "Loading character..."
    │   │
    │   └─ .character-img (initially hidden)
    │
    └─ .image-placeholder (SVG fallback)
        └─ <svg> (Marvel-branded graphic)
```

## 📈 Performance Characteristics

```
Best Case (Primary URL succeeds)
├─ Time: 0.5-2 seconds
├─ Attempts: 1
└─ User sees: Smooth fade-in

Average Case (1-2 fallbacks)
├─ Time: 2-4 seconds
├─ Attempts: 2-3
└─ User sees: Brief loading, then fade-in

Worst Case (All URLs fail)
├─ Time: 9-12 seconds (3s × 3-4 attempts)
├─ Attempts: 4
└─ User sees: Placeholder with character name
```

## 🌐 CDN Architecture

```
jsDelivr CDN (Primary)
├─ Global Edge Network
├─ 99.9% Uptime SLA
├─ Automatic HTTPS
├─ Browser Caching
└─ URLs: https://cdn.jsdelivr.net/gh/akabab/superhero-api@0.3.0/...
    ├─ /api/images/sm/620-spider-man.jpg
    ├─ /api/images/sm/346-iron-man.jpg
    └─ ... (all characters)
```

## 🔐 Security Layers

```
┌────────────────────────────────────────────────────┐
│  Layer 1: HTTPS Only                               │
│  ✅ All image URLs use HTTPS                       │
└────────────────────────────────────────────────────┘
┌────────────────────────────────────────────────────┐
│  Layer 2: CORS Handling                            │
│  ✅ crossOrigin="anonymous" attribute              │
└────────────────────────────────────────────────────┘
┌────────────────────────────────────────────────────┐
│  Layer 3: URL Validation                           │
│  ✅ Filter null/undefined URLs                     │
└────────────────────────────────────────────────────┘
┌────────────────────────────────────────────────────┐
│  Layer 4: Timeout Protection                       │
│  ✅ 3-second limit prevents indefinite loading     │
└────────────────────────────────────────────────────┘
┌────────────────────────────────────────────────────┐
│  Layer 5: Graceful Degradation                     │
│  ✅ SVG fallback always works (data URI)           │
└────────────────────────────────────────────────────┘
```

---

**Legend:**
- ✅ Success path
- ❌ Failure path
- 🔄 Processing
- ⏱️ Timeout protection
- 🎨 Visual element
- 📊 Data flow
